<div class="jumbotron">
  <h2>Yelp is the best way to find reviews on local restaurant.</h2>
  <h3>Explore the favorite dinners, pastry shops, and eateries in your community. <br>
  Leave reviews and rattings for your fellow foodies. </h3>
</div>

<table class='table table-hover table-condensed'>
  <thead>
    <tr>
      <th>Image</th>
      <th>Name</th>
      <th>Address</th>
      <th>Phone</th>
      <th>Website</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @restaurants.each do |restaurant| %>
      <tr>
        <td><%= image_tag restaurant.image_url, size: "250x150"  %></td>
        <td><%= restaurant.name  %></td>
        <td><%= restaurant.address %></td>
        <td><%= restaurant.phone %></td>
        <td><%= link_to restaurant.website,restaurant.website %></td>
        <td><%= link_to 'Show', restaurant, class:'btn btn-link' %></td>
        <td><%= link_to 'Edit', edit_restaurant_path(restaurant), class:'btn btn-link' %></td>
        <td><%= link_to 'Destroy', restaurant, method: :delete, data: { confirm: 'Are you sure?' }, class:'btn btn-link' %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Restaurant', new_restaurant_path %>
<%= image_tag 'https://s3.amazonaws.com/yelpdemobyme/happy_footer.png' %>